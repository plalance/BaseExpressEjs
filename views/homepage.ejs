<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8" />
        <title>Socket.io</title>
    </head>
 
    <body>

    <h1>Accueil</h1>

	<p>Exemple de route dynamique (paramètre GET) avec vue JES (ici étage 12):</p>
	<a href="/etage/12/chambre">/etage/12/chambre</a>
	
	<p>Passage de tablea à la vue, + boucle dans template EJS (ici 3 fois):</p>
	<a href="/compter/3">/compter/3</a>
	
	<h3>Todolist (en session)</h3>	
	<ul>
	
	<% todos.forEach(function(todo, index) { %>
		
		<li><a href="/remove_todo/<%= todo.id %>">✘</a> <%= todo.name %></li>
	
	<% }); %>
	</ul>

	<form action="/add_todo" method="POST">
		<input type="text" name="todoName">
		<input type="submit" value="save">	
	</form>
    </body>
</html>